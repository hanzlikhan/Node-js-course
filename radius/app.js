// app.js

// Import the radius value from constants.js
const { radius } = require("./constants");

// Import the calculateArea function from circleUtils.js
// const { calculateArea } = require("./circleUtils");

// // Use the function and display the result
// const area = calculateArea(radius);

console.log(`The area of a circle with radius ${radius} is ${area.toFixed(2)}.`);
